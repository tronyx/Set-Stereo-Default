# Optional: enables the live progress bars in set_stereo_default.py.
# Without it you still get each file's "[i/N]" line, just no bars.
tqdm>=4.60

# Non-Python dependencies (install separately, not via pip):
#   ffmpeg / ffprobe  https://ffmpeg.org
#   mkvmerge          https://mkvtoolnix.download  (only needed for .mkv/.webm files)
